<?php

    DEFINE ERROR_MSG DEBUG
    //DEFINE ERROR_MSG RELEASE
    
    public openlogfile()
    public closelogfile();
    
    public print_msg(msg,type) {
        if (filehandler!= null)
            if (ERROR_MSG is type)
                print_file(filehandler,datetimestamp+msg)
                
            
        
    }
class Parser
{
    //public function printini($file, $sector, $var)
    //{        
    //    $file= $file.".ini";
    //    $is=array();
    //    $is= parse_ini_file($file, true);
    //    //trim($is);
    //    //$is = array_map('trim',$is);
    //    if(is_array($is) && file_exists($file))
    //    {
    //        return trim($is[$sector][$var]);
    //    }else{
    //        return "error";
    //    }
    //    
    //}

    
}

?>